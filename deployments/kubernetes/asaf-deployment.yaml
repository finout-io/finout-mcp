apiVersion: apps/v1
kind: Deployment
metadata:
  name: asaf
  namespace: asaf
  labels:
    app: asaf
    component: diagnostic-tool
spec:
  replicas: 2
  selector:
    matchLabels:
      app: asaf
  template:
    metadata:
      labels:
        app: asaf
        component: diagnostic-tool
    spec:
      containers:
      - name: asaf
        image: 277411487094.dkr.ecr.us-east-1.amazonaws.com/asaf:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        env:
        # Database configuration (required)
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: asaf-secrets
              key: database-url
        # Finout API Configuration
        # Optional: only needed if MCP tools require direct Finout API auth.
        - name: FINOUT_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: asaf-secrets
              key: finout-client-id
              optional: true
        - name: FINOUT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: asaf-secrets
              key: finout-secret-key
              optional: true
        - name: FINOUT_INTERNAL_API_URL
          valueFrom:
            configMapKeyRef:
              name: asaf-config
              key: finout-internal-api-url
        - name: FINOUT_ACCOUNT_ID
          valueFrom:
            configMapKeyRef:
              name: asaf-config
              key: finout-default-account-id
        # Anthropic API Configuration
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: asaf-secrets
              key: anthropic-api-key
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
