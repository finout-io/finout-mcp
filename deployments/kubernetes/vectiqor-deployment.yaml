apiVersion: apps/v1
kind: Deployment
metadata:
  name: vectiqor
  namespace: vectiqor
  labels:
    app: vectiqor
    component: diagnostic-tool
spec:
  replicas: 2
  selector:
    matchLabels:
      app: vectiqor
  template:
    metadata:
      labels:
        app: vectiqor
        component: diagnostic-tool
    spec:
      containers:
      - name: vectiqor
        image: 277411487094.dkr.ecr.us-east-1.amazonaws.com/vectiqor:latest
        imagePullPolicy: Always
        command:
        - uvicorn
        - vectiqor.server:app
        - --host
        - 0.0.0.0
        - --port
        - "80"
        # Keep backend keepalive above ALB idle timeout to avoid connection reuse issues.
        - --timeout-keep-alive
        - "620"
        - --no-access-log
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        env:
        # Database configuration (required)
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: vectiqor-secrets
              key: database-url
        # Finout API Configuration
        # Optional: only needed if MCP tools require direct Finout API auth.
        - name: FINOUT_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: vectiqor-secrets
              key: finout-client-id
              optional: true
        - name: FINOUT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: vectiqor-secrets
              key: finout-secret-key
              optional: true
        - name: FINOUT_API_URL
          valueFrom:
            configMapKeyRef:
              name: vectiqor-config
              key: finout-internal-api-url
        # 0/-1/none disables timeout. Keep > typical long tool+LLM round-trips.
        - name: VECTIQOR_CHAT_TIMEOUT_SECONDS
          value: "540"
        # Anthropic API Configuration
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: vectiqor-secrets
              key: anthropic-api-key
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /api/health
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 6
