apiVersion: v1
kind: Service
metadata:
  name: vectiqor
  namespace: vectiqor
  labels:
    app: vectiqor
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: vectiqor
