apiVersion: v1
kind: Service
metadata:
  name: finout-mcp-hosted-public
  namespace: mcp-public
  labels:
    app: finout-mcp-hosted-public
spec:
  selector:
    app: finout-mcp-hosted-public
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
